# Copy this file to .env.local and set your Flask server's IP/port.
# Used in development so the React dev server knows where the API is.
# In production this is not needed — Nginx proxies /api/ to Gunicorn.
REACT_APP_API_BASE=http://192.168.0.x:5000
